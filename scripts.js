function generateNumber(){
const min = math.ceil(document.querySelector(".input-min").Value)

const max = math.floor(document.querySelector(".input-max").Value)

const result = Math.floor(Math.random() * (máx - min + 1)) + min;

alert(" result")
}